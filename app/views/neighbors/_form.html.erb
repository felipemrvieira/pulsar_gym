<%= form_with(model: neighbor) do |form| %>
  <% if neighbor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(neighbor.errors.count, "error") %> prohibited this neighbor from being saved:</h2>

      <ul>
        <% neighbor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :city_id, style: "display: block" %>
    <%= form.text_field :city_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

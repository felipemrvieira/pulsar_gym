<%= form_with(model: subscription_plan) do |form| %>
  <% if subscription_plan.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription_plan.errors.count, "error") %> prohibited this subscription_plan from being saved:</h2>

      <ul>
        <% subscription_plan.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :price_cents, style: "display: block" %>
    <%= form.number_field :price_cents %>
  </div>

  <div>
    <%= form.label :expiration, style: "display: block" %>
    <%= form.datetime_field :expiration %>
  </div>

  <div>
    <%= form.label :super_admin_id, style: "display: block" %>
    <%= form.text_field :super_admin_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

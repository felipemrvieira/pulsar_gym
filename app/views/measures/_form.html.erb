<%= form_with(model: measure) do |form| %>
  <% if measure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(measure.errors.count, "error") %> prohibited this measure from being saved:</h2>

      <ul>
        <% measure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body_part, style: "display: block" %>
    <%= form.text_field :body_part %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.number_field :value %>
  </div>

  <div>
    <%= form.label :examination_id, style: "display: block" %>
    <%= form.text_field :examination_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
